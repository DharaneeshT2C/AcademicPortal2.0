global class KenMetadataDeployCallback implements Metadata.DeployCallback {
    global void handleResult(Metadata.DeployResult result, Metadata.DeployCallbackContext context) {
        // No-op callback used to satisfy metadata deployment requirements.
    }
}